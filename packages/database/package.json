{
  "name": "database",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "npx prisma generate && npx prisma migrate deploy"
  },
  "devDependencies": {
    "prisma": "^4.8.0"
  },
  "dependencies": {
    "@prisma/client": "^4.8.0"
  }
}
