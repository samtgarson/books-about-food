{
  "name": "database",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "db:build": "npx prisma generate && npx prisma migrate deploy"
  },
  "prisma": {
    "schema": "prisma/schema.prisma",
    "seed": "./dump"
  },
  "devDependencies": {
    "prisma": "^5.1.0"
  },
  "dependencies": {
    "@prisma/client": "^5.1.0",
    "shared": "*"
  }
}
