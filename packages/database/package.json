{
  "name": "database",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "db:build": "npx prisma generate && npx prisma migrate deploy"
  },
  "devDependencies": {
    "prisma": "^4.9.0"
  },
  "dependencies": {
    "@prisma/client": "^4.9.0",
    "shared": "*"
  }
}
