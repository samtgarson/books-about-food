{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "start": {
      "outputs": []
    },
    "db:build": {
      "outputs": ["node_modules/.prisma/**"]
    },
    "build": {
      "dependsOn": ["^db:build"],
      "outputs": [".next/**"]
    },
    "dev": {
      "outputs": []
    }
  }
}
